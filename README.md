# Skill-Gap AI Platform

AI-driven Personalized Learning & Skill-Gap Analysis system.

## Setup
1. Backend: `cd backend && pip install -r requirements.txt && uvicorn app.main:app --reload`
2. Frontend: `cd frontend && npm install && npm start`
3. Docker: `cd docker && docker-compose up --build`
